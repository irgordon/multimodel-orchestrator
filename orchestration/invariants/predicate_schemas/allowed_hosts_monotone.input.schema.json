{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://orchestration/schemas/predicates/allowed_hosts_monotone.input",
  "title": "AllowedHostsMonotoneInput",
  "description": "Input structure for the allowed_hosts_monotone predicate.",
  "type": "object",
  "additionalProperties": false,
  "required": ["predicate_id", "config_path", "before_snapshot_hash", "after_snapshot_hash"],
  "properties": {
    "predicate_id": { "type": "string", "enum": ["allowed_hosts_monotone"] },
    "config_path": {
      "type": "string",
      "description": "Path to the network policy or allowed-hosts configuration file, relative to workspace root.",
      "minLength": 1
    },
    "before_snapshot_hash": {
      "type": "string",
      "description": "SHA-256 hex digest of the workspace snapshot before the task.",
      "pattern": "^[a-f0-9]{64}$"
    },
    "after_snapshot_hash": {
      "type": "string",
      "description": "SHA-256 hex digest of the workspace snapshot after the task.",
      "pattern": "^[a-f0-9]{64}$"
    }
  }
}
