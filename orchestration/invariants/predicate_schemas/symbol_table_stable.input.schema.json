{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://orchestration/schemas/predicates/symbol_table_stable.input",
  "title": "SymbolTableStableInput",
  "description": "Input structure for the symbol_table_stable predicate.",
  "type": "object",
  "additionalProperties": false,
  "required": ["predicate_id", "binary_path", "before_snapshot_hash", "after_snapshot_hash"],
  "properties": {
    "predicate_id": { "type": "string", "enum": ["symbol_table_stable"] },
    "binary_path": {
      "type": "string",
      "description": "Path to the compiled binary or shared library, relative to workspace root.",
      "minLength": 1
    },
    "before_snapshot_hash": {
      "type": "string",
      "description": "SHA-256 hex digest of the workspace snapshot before the task.",
      "pattern": "^[a-f0-9]{64}$"
    },
    "after_snapshot_hash": {
      "type": "string",
      "description": "SHA-256 hex digest of the workspace snapshot after the task.",
      "pattern": "^[a-f0-9]{64}$"
    }
  }
}
